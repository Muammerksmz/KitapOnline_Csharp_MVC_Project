@model KitapOnline.Models.Domain.Author
@{
}

<div class="container pt-5">
    <div class="row">
        <div class="col-md-6">
            <h3>Kitap Yazarı Ekleme</h3>
            <form asp-action="Add" asp-controller="Author" asp-area="Admin" method="post">
                <div class="form-group">
                    <label asp-for="AuthorName">Yazar ismi giriniz *</label>
                    <input type="text" class="form-control" asp-for="AuthorName" />
                    <span asp-validation-for="AuthorName" class="text-danger"></span>
                </div>

                @if (TempData["msg"] != null)
                {
                    <div class="form-group">
                        @TempData["msg"]
                    </div>
                }
                <div class="form-group pt-3">
                    <button type="submit" class="btn btn-dark"><i class="bi bi-save"></i> Kaydet</button>
                    <a href="/Admin/Author/GetAll" class="btn btn-primary"> <i class="bi bi-list"></i>Listele</a>
                </div>

            </form>
        </div>
    </div>
</div>

